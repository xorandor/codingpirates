| Type                    | Direction        | Format (one line, ends with `\n`) | Fields                                                               | Example                              | Notes                                                                  |
| ----------------------- | ---------------- | --------------------------------- | -------------------------------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------- |
| `JOINED`                | Client → Server  | `JOINED;<playerName>`             | `playerName` = name of the player                                    | `JOINED;Allan`                       | Server broadcasts to everyone else that player joined                  |
| `PING`                  | Client → Server  | `PING`                            | *(none)*                                                             | `PING`                               | Server can respond with `PONG` to confirm connection is alive.         |
| `MESSAGE`               | Client → Server  | `MESSAGE;<text>`                  | `text` = the rest of the line after the first `;`                    | `MESSAGE;Hello!`                     | Treat everything after the first `;` as text (no escaping needed yet). |
| `BOXMOVE`               | Client → Server  | `BOXMOVE;<boxId>;<x>;<y>`         | `boxId` = integer, `x`/`y` = integers                                | `BOXMOVE;1;100;300`                  | Server should validate and then broadcast an update.                   |
| `BOXMOVE_SERVER`               | Server → Client | `BOXMOVE;<boxId>;<x>;<y>`         | `boxId` = integer, `x`/`y` = integers                                | `BOXMOVE;1;100;300`                  | Client should accept the update from the server                   |
| `YOUARE`                | Server → Client  | `YOUARE;<ballId>`                 | `ballId` = integer assigned to this client                           | `YOUARE;3`                           | Sent by server immediately after JOINED to tell the client which ball is theirs |
| `BALL_JOINED`           | Server → Client  | `BALL_JOINED;<ballId>;<x>;<y>;<r>;<g>;<b>;<playerName>` | `ballId` = integer, `x`/`y` = starting position, `r`/`g`/`b` = color bytes, `playerName` = display name | `BALL_JOINED;3;450;200;255;80;30;Allan` | Broadcast when a new ball is created. Also sent directly to a new client for each existing ball so they see everyone |
| `BALL_LEFT`             | Server → Client  | `BALL_LEFT;<ballId>`              | `ballId` = integer of the ball to remove                             | `BALL_LEFT;3`                        | Broadcast when a client disconnects so all clients remove that ball |
| `DISCONNECTED`          | Internal (Server)| `DISCONNECTED;<playerName>`       | `playerName` = name of the disconnected player                       | `DISCONNECTED;Allan`                 | Queued internally by Networking when a client drops. Consumed by components on the server, never sent to clients |
